//
//  CardsFunctions.swift
//  UNO
//
//  Created by Aleksandra Front on 14/12/2021.
//

import Foundation

struct CardsFunctions {
    
    @available(*, unavailable) private init() {}
    
    static func reverse(card: CardModel, userCards: [CardModel], deck: DeckOfCards) -> [CardModel] {
        //wyslac do serwera reverseQueue hej
        return userCards
    }
    
    //DODAĆ RETURNY
    
    static func plusTwoCards(card: CardModel, userCards: [CardModel], deck: DeckOfCards) -> [CardModel] {
        
        
        return userCards
    }
    
    static func plusFourCards(card: CardModel, userCards: [CardModel], deck: DeckOfCards) -> [CardModel] {
        return userCards
    }
    
    static func stopPlayer(card: CardModel, userCards: [CardModel], deck: DeckOfCards) -> [CardModel] {
        return userCards
    }
    
    static func changeColor(card: CardModel, userCards: [CardModel], deck: DeckOfCards) -> [CardModel] {
        return userCards
    }
}
